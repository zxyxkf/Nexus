const sqlite = [
  `CREATE TABLE IF NOT EXISTS payment_selection_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    store TEXT NOT NULL,
    store_seq INTEGER NOT NULL,
    planner_id INTEGER NOT NULL,
    planner_name TEXT DEFAULT '',
    source_task_id INTEGER,
    source_task_no TEXT DEFAULT '',
    selection_date TEXT,
    style_number TEXT DEFAULT '',
    cost REAL,
    sale_price REAL,
    product_id TEXT DEFAULT '',
    selection_method TEXT DEFAULT '',
    detail_text TEXT DEFAULT '',
    design_main_image INTEGER DEFAULT 0,
    sku_le_200 INTEGER,
    listing_date TEXT,
    listing_category TEXT DEFAULT '',
    current_stage TEXT NOT NULL DEFAULT 'selection',
    process_status TEXT NOT NULL DEFAULT 'in_progress',
    end_stage TEXT,
    end_type TEXT,
    end_reason TEXT DEFAULT '',
    ended_at TEXT,
    version INTEGER NOT NULL DEFAULT 1,
    deleted_at TEXT,
    create_time TEXT DEFAULT (datetime('now', 'localtime')),
    update_time TEXT DEFAULT (datetime('now', 'localtime'))
  )`,
  `CREATE UNIQUE INDEX IF NOT EXISTS uk_payment_store_seq
    ON payment_selection_record(store, store_seq)`,
  `CREATE UNIQUE INDEX IF NOT EXISTS uk_payment_source_task
    ON payment_selection_record(source_task_id) WHERE source_task_id IS NOT NULL`,
  `CREATE INDEX IF NOT EXISTS idx_payment_status_store
    ON payment_selection_record(process_status, store, deleted_at)`,
  `CREATE TABLE IF NOT EXISTS payment_selection_image (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL,
    category TEXT NOT NULL,
    storage_root TEXT NOT NULL,
    relative_path TEXT NOT NULL,
    original_name TEXT DEFAULT '',
    mime_type TEXT DEFAULT '',
    file_size INTEGER DEFAULT 0,
    sort_order INTEGER DEFAULT 0,
    adjustment_id INTEGER,
    source_task_file_id INTEGER,
    uploader_id INTEGER,
    deleted_at TEXT,
    create_time TEXT DEFAULT (datetime('now', 'localtime'))
  )`,
  `CREATE INDEX IF NOT EXISTS idx_payment_image_record
    ON payment_selection_image(record_id, category, deleted_at, sort_order)`,
  `CREATE INDEX IF NOT EXISTS idx_payment_image_adjustment
    ON payment_selection_image(record_id, category, adjustment_id, deleted_at, sort_order)`,
  `CREATE TABLE IF NOT EXISTS payment_selection_stage (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL,
    stage_code TEXT NOT NULL,
    stage_status TEXT NOT NULL DEFAULT 'active',
    is_reopened INTEGER DEFAULT 0,
    entered_at TEXT DEFAULT (datetime('now', 'localtime')),
    completed_at TEXT,
    UNIQUE(record_id, stage_code)
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_preparation (
    record_id INTEGER PRIMARY KEY,
    review_count INTEGER,
    new_ops_registered INTEGER,
    paid_enabled INTEGER,
    paid_at TEXT
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_testing (
    record_id INTEGER PRIMARY KEY,
    paid_enabled INTEGER,
    paid_at TEXT,
    promotion_method TEXT DEFAULT '',
    car_promotion_method TEXT DEFAULT '',
    car_clicks INTEGER,
    car_ctr REAL,
    car_qualifies INTEGER,
    site_promotion_method TEXT DEFAULT '',
    overall_visitors INTEGER,
    search_visitors INTEGER,
    buyers INTEGER,
    average_ctr REAL,
    potential_status TEXT DEFAULT '',
    unqualified_action TEXT DEFAULT '',
    manager_report_date TEXT,
    wei_stock_reported INTEGER
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_monitoring (
    record_id INTEGER PRIMARY KEY,
    link_optimized INTEGER,
    link_status TEXT DEFAULT '',
    domestic_sales_count INTEGER,
    added_reviews INTEGER,
    title_optimized_at TEXT,
    qa_count INTEGER,
    detail_optimized_at TEXT,
    material_selected INTEGER,
    sku_optimized_at TEXT,
    campaign_name TEXT DEFAULT '',
    concession_rate TEXT,
    quick_peak_done INTEGER,
    abandoned INTEGER DEFAULT 0,
    abandon_reason TEXT DEFAULT '',
    abandon_at TEXT
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_adjustment (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL,
    client_key TEXT,
    sort_order INTEGER NOT NULL,
    reason TEXT DEFAULT '',
    adjusted_at TEXT,
    fee_ratio_7d REAL,
    payers_7d INTEGER,
    total_budget REAL,
    detail_text TEXT DEFAULT '',
    feedback_text TEXT DEFAULT '',
    UNIQUE(record_id, sort_order),
    UNIQUE(record_id, client_key)
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_breakout (
    record_id INTEGER PRIMARY KEY,
    pit_output_day1 REAL,
    pit_output_day2 REAL,
    pit_output_day3 REAL,
    flash_sale_at TEXT,
    super_breakout_at TEXT,
    rapid_breakout_at TEXT,
    strong_lift_qualified INTEGER,
    search_growth_trend TEXT DEFAULT '',
    payer_trend TEXT DEFAULT '',
    current_budget REAL,
    fee_ratio_7d REAL,
    payers_7d INTEGER,
    adjusted_at TEXT,
    total_budget REAL,
    detail_text TEXT DEFAULT '',
    feedback_text TEXT DEFAULT ''
  )`,
  `CREATE TABLE IF NOT EXISTS payment_selection_summary (
    record_id INTEGER PRIMARY KEY,
    exploded INTEGER,
    link_maintenance TEXT DEFAULT '',
    style_definition TEXT DEFAULT '',
    summary_text TEXT DEFAULT '',
    notes TEXT DEFAULT ''
  )`,
  `CREATE TABLE IF NOT EXISTS payment_listing_category (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    sort_order INTEGER DEFAULT 0,
    active INTEGER DEFAULT 1,
    create_time TEXT DEFAULT (datetime('now', 'localtime')),
    update_time TEXT DEFAULT (datetime('now', 'localtime'))
  )`,
  `CREATE TABLE IF NOT EXISTS payment_promotion_method (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    sort_order INTEGER DEFAULT 0,
    active INTEGER DEFAULT 1,
    create_time TEXT DEFAULT (datetime('now', 'localtime')),
    update_time TEXT DEFAULT (datetime('now', 'localtime'))
  )`
];

const mysql = [
  `CREATE TABLE IF NOT EXISTS payment_selection_record (
    id INT AUTO_INCREMENT PRIMARY KEY,
    store VARCHAR(100) NOT NULL,
    store_seq INT NOT NULL,
    planner_id INT NOT NULL,
    planner_name VARCHAR(100) DEFAULT '',
    source_task_id INT DEFAULT NULL,
    source_task_no VARCHAR(100) DEFAULT '',
    selection_date DATETIME DEFAULT NULL,
    style_number VARCHAR(100) DEFAULT '',
    cost DECIMAL(14,4) DEFAULT NULL,
    sale_price DECIMAL(14,4) DEFAULT NULL,
    product_id VARCHAR(200) DEFAULT '',
    selection_method VARCHAR(200) DEFAULT '',
    detail_text TEXT,
    design_main_image TINYINT DEFAULT 0,
    sku_le_200 TINYINT DEFAULT NULL,
    listing_date DATETIME DEFAULT NULL,
    listing_category VARCHAR(200) DEFAULT '',
    current_stage VARCHAR(30) NOT NULL DEFAULT 'selection',
    process_status VARCHAR(30) NOT NULL DEFAULT 'in_progress',
    end_stage VARCHAR(30) DEFAULT NULL,
    end_type VARCHAR(50) DEFAULT NULL,
    end_reason VARCHAR(500) DEFAULT '',
    ended_at DATETIME DEFAULT NULL,
    version INT NOT NULL DEFAULT 1,
    deleted_at DATETIME DEFAULT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_payment_store_seq (store, store_seq),
    UNIQUE KEY uk_payment_source_task (source_task_id),
    KEY idx_payment_status_store (process_status, store, deleted_at)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_image (
    id INT AUTO_INCREMENT PRIMARY KEY,
    record_id INT NOT NULL,
    category VARCHAR(30) NOT NULL,
    storage_root VARCHAR(1000) NOT NULL,
    relative_path VARCHAR(1000) NOT NULL,
    original_name VARCHAR(500) DEFAULT '',
    mime_type VARCHAR(100) DEFAULT '',
    file_size INT DEFAULT 0,
    sort_order INT DEFAULT 0,
    adjustment_id INT DEFAULT NULL,
    source_task_file_id INT DEFAULT NULL,
    uploader_id INT DEFAULT NULL,
    deleted_at DATETIME DEFAULT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_payment_image_record (record_id, category, deleted_at, sort_order),
    KEY idx_payment_image_adjustment (record_id, category, adjustment_id, deleted_at, sort_order)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_stage (
    id INT AUTO_INCREMENT PRIMARY KEY,
    record_id INT NOT NULL,
    stage_code VARCHAR(30) NOT NULL,
    stage_status VARCHAR(30) NOT NULL DEFAULT 'active',
    is_reopened TINYINT DEFAULT 0,
    entered_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME DEFAULT NULL,
    UNIQUE KEY uk_payment_stage (record_id, stage_code)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_preparation (
    record_id INT PRIMARY KEY,
    review_count INT DEFAULT NULL,
    new_ops_registered TINYINT DEFAULT NULL,
    paid_enabled TINYINT DEFAULT NULL,
    paid_at DATETIME DEFAULT NULL
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_testing (
    record_id INT PRIMARY KEY,
    paid_enabled TINYINT DEFAULT NULL,
    paid_at DATETIME DEFAULT NULL,
    promotion_method VARCHAR(200) DEFAULT '',
    car_promotion_method VARCHAR(200) DEFAULT '',
    car_clicks INT DEFAULT NULL,
    car_ctr DECIMAL(14,4) DEFAULT NULL,
    car_qualifies TINYINT DEFAULT NULL,
    site_promotion_method VARCHAR(200) DEFAULT '',
    overall_visitors INT DEFAULT NULL,
    search_visitors INT DEFAULT NULL,
    buyers INT DEFAULT NULL,
    average_ctr DECIMAL(14,4) DEFAULT NULL,
    potential_status VARCHAR(30) DEFAULT '',
    unqualified_action VARCHAR(100) DEFAULT '',
    manager_report_date DATETIME DEFAULT NULL,
    wei_stock_reported TINYINT DEFAULT NULL
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_monitoring (
    record_id INT PRIMARY KEY,
    link_optimized TINYINT DEFAULT NULL,
    link_status VARCHAR(30) DEFAULT '',
    domestic_sales_count INT DEFAULT NULL,
    added_reviews INT DEFAULT NULL,
    title_optimized_at DATETIME DEFAULT NULL,
    qa_count INT DEFAULT NULL,
    detail_optimized_at DATETIME DEFAULT NULL,
    material_selected TINYINT DEFAULT NULL,
    sku_optimized_at DATETIME DEFAULT NULL,
    campaign_name VARCHAR(100) DEFAULT '',
    concession_rate VARCHAR(20) DEFAULT '',
    quick_peak_done TINYINT DEFAULT NULL,
    abandoned TINYINT DEFAULT 0,
    abandon_reason VARCHAR(500) DEFAULT '',
    abandon_at DATETIME DEFAULT NULL
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_adjustment (
    id INT AUTO_INCREMENT PRIMARY KEY,
    record_id INT NOT NULL,
    client_key VARCHAR(100) DEFAULT NULL,
    sort_order INT NOT NULL,
    reason VARCHAR(500) DEFAULT '',
    adjusted_at DATETIME DEFAULT NULL,
    fee_ratio_7d DECIMAL(14,4) DEFAULT NULL,
    payers_7d INT DEFAULT NULL,
    total_budget DECIMAL(14,4) DEFAULT NULL,
    detail_text TEXT,
    feedback_text TEXT,
    UNIQUE KEY uk_payment_adjustment_order (record_id, sort_order),
    UNIQUE KEY uk_payment_adjustment_client (record_id, client_key)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_breakout (
    record_id INT PRIMARY KEY,
    pit_output_day1 DECIMAL(14,4) DEFAULT NULL,
    pit_output_day2 DECIMAL(14,4) DEFAULT NULL,
    pit_output_day3 DECIMAL(14,4) DEFAULT NULL,
    flash_sale_at DATETIME DEFAULT NULL,
    super_breakout_at DATETIME DEFAULT NULL,
    rapid_breakout_at DATETIME DEFAULT NULL,
    strong_lift_qualified TINYINT DEFAULT NULL,
    search_growth_trend VARCHAR(50) DEFAULT '',
    payer_trend VARCHAR(50) DEFAULT '',
    current_budget DECIMAL(14,4) DEFAULT NULL,
    fee_ratio_7d DECIMAL(14,4) DEFAULT NULL,
    payers_7d INT DEFAULT NULL,
    adjusted_at DATETIME DEFAULT NULL,
    total_budget DECIMAL(14,4) DEFAULT NULL,
    detail_text TEXT,
    feedback_text TEXT
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_selection_summary (
    record_id INT PRIMARY KEY,
    exploded TINYINT DEFAULT NULL,
    link_maintenance VARCHAR(50) DEFAULT '',
    style_definition VARCHAR(50) DEFAULT '',
    summary_text TEXT,
    notes TEXT
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_listing_category (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(200) NOT NULL UNIQUE,
    sort_order INT DEFAULT 0,
    active TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
  `CREATE TABLE IF NOT EXISTS payment_promotion_method (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(200) NOT NULL UNIQUE,
    sort_order INT DEFAULT 0,
    active TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`
];

module.exports = { sqlite, mysql };
